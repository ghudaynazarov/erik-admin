<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= pageTitle %></title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/auth.css">
</head>

<body>
    <main style="position: absolute;width: 100%;height: 100%;display: flex;justify-content: center;align-items: center;background-color: white;">
        <div style="padding: 50px;border: 1px solid #8b8b8b63;border-radius: 10px;background-color: #f7f7f7;    box-shadow: 5px 7px 17px 3px gray;">
            <% if(errorMassage) { %>
                <div style="color: rgb(211, 38, 38);" class="user-message user-message--error"><%= errorMassage %></div>
            <% } %>
            <form class="login-form" action="/login" method="POST" novalidate>
                <div class="form-control">
                    <label for="email">E-Mail</label>
                    <input 
                    class="<%= validationErrors.find(e => e.param === 'email') ? 'invalid' : '' %>"
                    type="text" 
                    name="email" 
                    id="email"
                    style="padding: 6px;"
                    value="<%= oldInput.email %>" required>
                </div> 
                <div class="form-control">
                    <label for="password">Password</label>
                    <input
                    class="<%= validationErrors.find(e => e.param === 'password') ? 'invalid' : '' %>"
                    type="password" 
                    name="password" 
                    id="password"
                    style="padding: 6px;"
                    value="<%= oldInput.password %>" required>
                </div>
                <button class="btn" type="submit">Login</button>
            </form>
            <!-- <div class="centered">
                <a href="/reset">Reset Password</a>
            </div> -->
        </div>
       
    </main>
</body>

</html>